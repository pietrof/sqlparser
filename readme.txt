to generate, ran this in copilot:
create a .dox file which includes documentation for each method in this file

the output is then saved to a file called `myfile.dox`

the dox file is then processed by doxygen to generate the documentation as part of the github action.

